import 'package:flutter/material.dart';

class InputLocationScreen extends StatelessWidget {
  const InputLocationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Location"),
      ),
    );
  }
}
